// TODO: Phase 6 — Build from flixscout-final.html .rec-card styles
// Includes: poster, title, year, genre tags, rating, runtime, platform SVG badge
// Expand/collapse synopsis with max-height animation via Reanimated
// Hover lift: translateY(-1) on press

import { View } from 'react-native';

export default function RecommendationCard() {
  return <View />;
}
