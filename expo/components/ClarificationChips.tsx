// TODO: Phase 6 — Build from flixscout-final.html .chip / .clarify-block styles
// Multi-select chip rows per question
// "Scout it" continue button visible only when ≥1 chip selected per question

import { View } from 'react-native';

export default function ClarificationChips() {
  return <View />;
}
